import 'package:flutter/material.dart';

class Jepang extends StatelessWidget {
  const Jepang ({Key? key}) :super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ini halaman Jepang", style: TextStyle(fontSize: 50),),
      ),
    );
  }
}